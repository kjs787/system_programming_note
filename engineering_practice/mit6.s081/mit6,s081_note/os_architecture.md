  

# 操作系统隔离性

应用程序常常不互相信任，且出现bug，我们期望应用程序之间有更强的**隔离性，一个程序的运行状态不能影响其他程序**。

为了实现这种隔离性，**应用程序不应直接访问敏感的硬件资源**，而是通过操作系统**将硬件资源抽象成服务**，也就是**系统调用**。

操作系统向应用程序提供了**“进程”**这一抽象，**应用程序不直接访问cpu，而是与进程进行交互**。通过保存和恢复进程的寄存器状态，进程好像在独享系统资源，而应用程序不必意识到分时共享的存在。

操作系统对**内存资源**和**处理器资源**的操作是相当自由的

![](https://cdn.nlark.com/yuque/0/2026/png/63601471/1780922823244-667103f5-b7b8-4183-9d6e-2bdaadb5f3bc.png)

类unix操作系统使用文件描述符，进行进程间的交互，文件描述符抽象了很多细节，简化了交互模式

  

# 操作系统防御性

**硬件支持强隔离性的两个特征：**

1. 用户态/内核态
2. 虚拟内存和页表

RISC-V有三种CPU可以执行指令的模式：**机器模式(Machine Mode)、用户模式(User Mode)和管理模式(Supervisor Mode)**。不同的状态可以执行不同权限的指令，这在保证强隔离性时是有必要的

用户程序只会在用户态执行，当他想执行一个特权指令时，cpu不会执行该指令，而是切换到内核态，以便操作系统终止该进程，因为它做了它不应该做的事情。

当用户态程序向调用内核程序时，比如使用系统调用，CPU提供一个特殊的指令，将CPU从用户模式切换到管理模式，并**在内核指定的入口点进入内核**（RISC-V为此提供`ecall`指令）。内核可以验证系统调用的参数，决定该不该执行该操作。

值得注意的是，**用户程序不能决定内核入口点**，否则恶意程序可以跳过参数验证进入内核。

  

**用户态和内核态的切换：**

在RISC-V中，有一个专门的指令用来实现这个功能，叫做**ECALL**。ECALL接收一个数字参数，当一个用户程序想要将程序执行的控制权转移到内核，它只需要执行ECALL指令，并传入一个数字。这里的数字参数代表了应用程序想要调用的System Call。**这是用户程序执行系统调用的唯一方法。**

  

syscall.c的函数syscall，**每一个从应用程序发起的系统调用都会调用到这个syscall函数**，syscall函数会检查ECALL的参数，决定是否执行内核中的系统调用。（syscall在内核侧，且内核侧可以进行参数检查）

  

# 内核组织

这个看看概念就行，要知道xv6是一个**宏内核操作系统**。

![](https://cdn.nlark.com/yuque/0/2026/png/63601471/1780923834793-dadf2a30-c5dc-48cb-8bbd-4222f13fa182.png)

  

**微内核中shell向文件系统请求信息的过程，可以看出微内核的特点与缺陷：**

![](https://cdn.nlark.com/yuque/0/2026/png/63601471/1781076624908-48b37dcb-af0b-40fa-a709-f645ba38991c.png)

# QEMU硬件仿真

QEMU就是一个c程序，在一个无限循环中，做这些事：

- 读取4字节或者8字节的RISC-V指令。
- 解析RISC-V指令，并找出对应的操作码（op code）。我们之前在看kernel.asm的时候，看过一些操作码的二进制版本。通过解析，或许可以知道这是一个ADD指令，或者是一个SUB指令。
- 之后，在软件中执行相应的指令。

QEMU还会在主循环中仿真寄存器状态，仿真所有的特权/非特权指令。

为了仿真4个核，他使用多线程实现并行处理

  

当你使用QEMU时，应该想象他是一块真正的主板，上面存在一个RISC-V处理器：

![](https://cdn.nlark.com/yuque/0/2026/png/63601471/1781077439245-1520846d-aa35-4445-906d-82db795d8c36.png)

![](https://cdn.nlark.com/yuque/0/2026/png/63601471/1781077451560-1316c795-0462-443f-a88b-6741947960de.png)

这个图里面有：

- 4个核：U54 Core 1-4
- L2 cache：Banked L2
- 连接DRAM的连接器：DDR Controller
- 各种连接外部设备的方式，比如说UART0，一端连接了键盘，另一端连接了terminal。
- 以及连接了时钟的接口：Clock Generation

  

# XV6架构

![](https://cdn.nlark.com/yuque/0/2026/png/63601471/1780923938838-dda16afb-9457-4383-8934-7acf06e1f05c.png)

# 进程抽象

为了帮助加强隔离，进程的抽象给程序提供了一种错觉，即它有自己的专用机器。进程为程序提供了一个看起来像是私有内存系统或地址空间的东西，其他进程不能读取或写入。进程还为程序提供了看起来像是自己的CPU来执行程序的指令。

RISC-V用页表把进程的虚拟地址转换为物理地址（CPU芯片发送到主存储器的地址），每个进程的页表是独立的，因而不同进程不会访问不属于他的资源。

![](https://cdn.nlark.com/yuque/0/2026/png/63601471/1780926072722-186f9cd7-55b8-4219-8c41-54d812a8925b.png)

RISC-V上的指针有64位宽；硬件在页表中查找虚拟地址时只使用低39位；xv6只使用这39位中的38位。因此，最大地址是2^38-1=0x3fffffffff，即`MAXVA`（定义在_**kernel/riscv.h**_:348）。

->在下一小结我们会深入探索页表

  

xv6内核维护进程的许多状态片段，将其保存在一个叫 proc 的结构体中。一个进程最重要的内核状态片段是它的**页表、内核栈区和运行状态**

```
// Per-process state
struct proc {
    struct spinlock lock;

    // p->lock must be held when using these:
    enum procstate state;        // Process state
    struct proc *parent;         // Parent process
    void *chan;                  // If non-zero, sleeping on chan
    int killed;                  // If non-zero, have been killed
    int xstate;                  // Exit status to be returned to parent's wait
    int pid;                     // Process ID

    // these are private to the process, so p->lock need not be held.
    uint64 kstack;               // Virtual address of kernel stack
    uint64 sz;                   // Size of process memory (bytes)
    pagetable_t pagetable;       // User page table
    struct trapframe *trapframe; // data page for trampoline.S
    struct context context;      // swtch() here to run process
    struct file *ofile[NOFILE];  // Open files
    struct inode *cwd;           // Current directory
    char name[16];               // Process name (debugging)
};
```

`p->state`表明进程是已分配、就绪态、运行态、等待I/O中（阻塞态）还是退出

`p->pagetable`以RISC-V硬件所期望的格式保存进程的页表

  

执行线程

每个进程都有一个**执行线程**（或简称线程）来执行进程的指令。一个线程可以挂起并且稍后再恢复。为了透明地在进程之间切换，内核挂起当前运行的线程，并恢复另一个进程的线程。**线程的大部分状态（本地变量、函数调用返回地址）存储在线程的栈区上**。每个进程有两个栈区：一个**用户栈区**和一个**内核栈区**（`p->kstack`）。当进程执行用户指令时，只有它的用户栈在使用，它的内核栈是空的。当进程进入内核（由于系统调用或中断）时，内核代码在进程的内核堆栈上执行；当一个进程在内核中时，它的用户堆栈仍然包含保存的数据，只是不处于活动状态。**进程的线程在主动使用它的用户栈和内核栈之间交替**。**内核栈是独立的**（并且不受用户代码的保护），因此即使一个进程破坏了它的用户栈，内核依然可以正常运行。

  

# 启动xv6的过程

xv6启动

了使xv6更加具体，我们将概述内核如何启动和运行第一个进程。接下来的章节将更详细地描述本概述中显示的机制。

当RISC-V计算机上电时，它会初始化自己并运行一个存储在只读内存中的引导加载程序。引导加载程序将xv6内核加载到内存中。然后，在机器模式下，中央处理器从`_entry` (_**kernel/entry.S**_:6)开始运行xv6。Xv6启动时页式硬件（paging hardware）处于禁用模式：也就是说虚拟地址将直接映射到物理地址。

加载程序将xv6内核加载到物理地址为`0x80000000`的内存中。它将内核放在`0x80000000`而不是`0x0`的原因是地址范围`0x0:0x80000000`包含I/O设备。

`_entry`的指令设置了一个栈区，这样xv6就可以运行C代码。Xv6在_**start. c (kernel/start.c:11)**_文件中为初始栈_**stack0**_声明了空间。由于RISC-V上的栈是向下扩展的，所以`_entry`的代码将栈顶地址`stack0+4096`加载到栈顶指针寄存器`sp`中。现在内核有了栈区，`_entry`便调用C代码`start`(_**kernel/start.c**_:21)。

函数`start`执行一些仅在机器模式下允许的配置，然后切换到管理模式。RISC-V提供指令`mret`以进入管理模式，该指令最常用于将管理模式切换到机器模式的调用中返回。而`start`并非从这样的调用返回，而是执行以下操作：它在寄存器`mstatus`中将先前的运行模式改为管理模式，它通过将`main`函数的地址写入寄存器`mepc`将返回地址设为`main`，它通过向页表寄存器`satp`写入0来在管理模式下禁用虚拟地址转换，并将所有的中断和异常委托给管理模式。

在进入管理模式之前，`start`还要执行另一项任务：对时钟芯片进行编程以产生计时器中断。清理完这些“家务”后，`start`通过调用`mret`“返回”到管理模式。这将导致程序计数器（PC）的值更改为`main`(_**kernel/main.c**_:11)函数地址。

**TIPS**

**注：**`mret`执行返回，返回到先前状态，由于`start`函数将前模式改为了管理模式且返回地址改为了`main`,因此`mret`将返回到`main`函数，并以管理模式运行

在`main`(_**kernel/main.c**_:11)初始化几个设备和子系统后，便通过调用`userinit` (_**kernel/proc.c**_:212)创建第一个进程，第一个进程执行一个用RISC-V程序集写的小型程序：_**initcode. S**_ (_**user/initcode.S:**_1)，它通过调用`exec`系统调用重新进入内核。正如我们在第1章中看到的，`exec`用一个新程序（本例中为 `/init`）替换当前进程的内存和寄存器。一旦内核完成`exec`，它就返回`/init`进程中的用户空间。如果需要，`init`(_**user/init.c**_:15)将创建一个新的控制台设备文件，然后以文件描述符0、1和2打开它。然后它在控制台上启动一个shell。系统就这样启动了。

